# QSO-021 Shipped — lint --sync Drift Detection

**Date:** 2026-08-15

## What shipped

`qsos lint --sync` adds drift checks on top of static lint:

- **adr-unlinked** — Accepted ADR not referenced in `architecture.dsl`
- **dsl-unjustified** — DSL element not backed by Accepted ADR
- **import-drift** — cross-container import without DSL relationship

Container source dirs convention: `src/{container_id}/`

## Unblocks

Full `/qsos-doc-sync` Step 6c close gate with `--sync`.

## Next

QSO-028 (pre-commit hooks) or QSO-025 (MCP)
