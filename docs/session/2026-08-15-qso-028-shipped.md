# Session — QSO-028 pre-commit hooks shipped

**Date:** 2026-08-15

## Shipped

- `qsos init --hooks` — installs `.git/hooks/pre-commit` with marker `qsos-pre-commit-hook v1`
- `qsos init --hooks --baseline` — writes `.audit-baseline.json` from current lint errors before hook install
- `qsos init --check` — reports hook installed/missing (when not running full scaffold check)
- `qsos lint --staged` — lints only git-indexed files under `docs/features/` and `docs/decisions/`
- Baseline filtering in `qsos-lint` suppresses known violations on staged lint

## Evidence

`work/QSO-028-qsos-init-precommit/evidence/`

- `init-hook-staged.json` — blank staged feature → `feature-not-empty` violation
- `init-hooks-install.json` — hook install report + script body
- `init-check-hooks.json` — check mode confirms hook present
- `init-hook-block.txt` — commit rejected (`commit_exit=1`) when staged file violates lint
- `init-hook-baseline.json` — baseline allows commit when only pre-existing violations remain

## Tests

18 unit tests pass (`qsos-init` 7, `qsos-lint` 11).

## Next

QSO-025 (MCP server) or QSO-024 (watcher hub) — only open utilities tickets.
