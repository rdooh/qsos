# QSO-030 Shipped — qsos init Setup Wizard

**Date:** 2026-08-15

## What shipped

New `qsos-init` crate + CLI subcommand:

```bash
qsos init --name my-poc --prefix POC- [--description "..."] [--test-runner pytest]
qsos init --check --root .
qsos init --dry-run --name my-poc --prefix POC-
```

Scaffolds: directory tree, `catalog-mesh.yaml`, `architecture.dsl`, `tix-manifest.json`, `testing/manifest.json`, quadrant READMEs, `.gitignore`. Idempotent — skips existing paths.

## Verification

5 unit tests pass; scaffolded project passes `qsos lint` with zero errors.

Evidence: `work/QSO-030-new-project-setup-wizard/evidence/`

## Also updated

- `/qsos-orient` — run `qsos init --check` on unfamiliar repos

## Next

QSO-026 (test ingestion) or QSO-021 (`lint --sync`)
